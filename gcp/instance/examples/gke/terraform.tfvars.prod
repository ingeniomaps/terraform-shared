# ============================================================================
# CONFIGURACIÓN PARA AMBIENTE: PRODUCTION
# ============================================================================

# ============================================================================
# CONFIGURACIÓN DEL PROYECTO
# ============================================================================
project_id  = "tu-proyecto-dev-123456"
region      = "us-central1"

# Opcional: Nombre del archivo JSON de credenciales (debe estar en ../keys/)
# Si no se especifica, se usa GOOGLE_APPLICATION_CREDENTIALS o gcloud auth
# credentials_file = "account_service_prod.json"

# ============================================================================
# ESTADO DE LA INFRAESTRUCTURA COMPARTIDA
# ============================================================================
# Bucket donde está el estado de shared-infra
shared_infra_state_bucket = "terraform-state-prod"

# Prefijo del estado (debe coincidir con el backend de shared-infra)
shared_infra_state_prefix = "shared/terraform"

# ============================================================================
# CONFIGURACIÓN DEL CLUSTER
# ============================================================================
cluster_name = "gke-prod"

# CIDR del control plane (debe coincidir con shared-infra)
gke_master_cidr = "172.16.0.0/28"

# ============================================================================
# CONFIGURACIÓN DE NODOS
# ============================================================================
node_pool_name    = "default-pool"
node_machine_type = "e2-standard-4"  # Máquina más potente para producción
node_disk_size    = 100
node_disk_type    = "pd-ssd"  # SSD para mejor rendimiento en producción

# Auto-scaling
min_node_count     = 3
max_node_count     = 10
initial_node_count = 3
enable_autoscaling = true

# Mantenimiento
enable_autorepair  = true
enable_autoupgrade = true

# Versión de Kubernetes (especificar versión estable para producción)
kubernetes_version = ""  # Dejar vacío para última estable, o especificar versión

# ============================================================================
# SEGURIDAD Y RED
# ============================================================================
# Nodos privados (sin IP pública)
enable_private_nodes = true

# Endpoint privado del control plane (requiere VPN/VPC Peering para acceso)
enable_private_endpoint = false

# Redes autorizadas para acceder al control plane (recomendado para producción)
# master_authorized_networks = {
#   "10.0.0.0/8" = "Red corporativa"
# }

# ============================================================================
# FEATURES
# ============================================================================
enable_http_load_balancing        = true
enable_horizontal_pod_autoscaling = true
enable_network_policy             = true  # Habilitado para producción

# ============================================================================
# MANTENIMIENTO
# ============================================================================
maintenance_window_start_time = "02:00"
maintenance_window_day        = "SUNDAY"

# ============================================================================
# LABELS Y TAGS
# ============================================================================
labels = {
  environment = "prod"
  team        = "mi-equipo"
  managed_by  = "terraform"
}

network_tags = []
